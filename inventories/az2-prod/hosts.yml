---
all:
  children:
    headnode:
      hosts:
        az2-prod-head:
          ansible_host: "{{ bcm_head_ssh_ip }}"
          ansible_user: root
    compute:
      vars:
        ansible_user: root
        ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
      children:
        gpu_nodes: {}
        cpu_nodes: {}
